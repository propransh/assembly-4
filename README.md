# Assembly

Multi-agent debate simulation engine.

## Structure
- `frontend/` — Vue.js frontend (Hamza)
- `backend/` — Python simulation engine (Pransh)

## Frontend
cd frontend && npm install && npm run dev

## Backend
cd backend && (Pransh adds instructions here)